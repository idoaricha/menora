package extensions;


import io.qameta.allure.Step;
import utilities.CommonOps;
import java.util.ArrayList;
import java.util.List;

public class DbActions extends CommonOps {
/*
    @Step("get Data from database")
    public static List<String> getDBData(String query){
        List<String> Data = new ArrayList<String>();
        try {
            rs = stat.executeQuery(query);
            while(rs.next()){ //while there is more row on table return the next line
                Data.add(rs.getString(1));
                Data.add(rs.getString(2));
            }
        } catch (Exception e) {
            System.out.println("Error Occur While Execute The Query. see details : " + e);
        }
        return Data;
    }
    */

}
